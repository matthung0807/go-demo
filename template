<!DOCTYPE html>
<html>
<head>
<style>
table, th, td {
  border: solid 1px #888;
  padding: 8px;
}

th {
  color: #eee;
  background: #111;
}

table {
    border-collapse:collapse;
}

tr:nth-child(even) {
  background: #eee;
}

tr:hover {
  background: #ddd;
}
</style>
</head>
<body>
    <h1>{{.Name}}</h1>
    <table>
        <thead>
            <tr>
                <th>編號</th>
                <th>名稱</th>
                <th>年齡</th>
            </tr>
        </thead>
        <tbody>
            {{range .Employees}}
            <tr>
                <td>{{.Id}}</td>
                <td>{{.Name}}</td>
                <td>{{.Age}}</td>
            </tr>
            {{end}}
        </tbody>
    </table>
</body>
</html>